/* Extracted from legacy/components/swiper — do not edit by hand; re-run scripts/extract.mjs */
/* --- index.vue --- */
.cu-swiper-box
  overflow hidden
  will-change tranform
.cu-swiper, .cu-swiper-box
  width 100%
  height 100%
  position relative
  &.disabled
    visibility hidden
  &.cu-swiper-fade
    .cu-swiper-item
      position absolute
      opacity 0
      top 0
      left 0
  &.cu-swiper-vertical
    .cu-swiper-container
      width 100%
      height auto
      box-orient vertical
      flex-direction column
    .cu-swiper-indicators
      flex-direction column
      right 20px
      left auto
      bottom auto
      top 50%
      transform translate(0, -50%)
      &.disabled
        visibility hidden
      .cu-swiper-indicator
        width 4px
        height 16px
        margin 2.5px 0

.cu-swiper-container
  height 100%
  width auto
  position relative
  display flex
  box-sizing content-box

.cu-swiper-indicators
    position absolute
    bottom 20px
    left 50%
    display flex
    transform translateX(-50%)

.cu-swiper-indicator
  width 16px
  height 4px
  display inline-block
  background #ddd
  margin 0 3px
  &.cu-swiper-indicator-active
    background swiper-indicator-fill

/* --- swiper-item.vue --- */
  .cu-swiper-item
    position relative
    width 100%
    flex-shrink 0
