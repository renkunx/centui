/* Extracted from legacy/components/action-sheet — do not edit by hand; re-run scripts/extract.mjs */
/* --- action-sheet.vue --- */
.cu-action-sheet
  color action-sheet-color
  -webkit-font-smoothing antialiased
  .cu-popup
    z-index action-sheet-zindex
  .cu-popup-box
    background-color color-bg-base

.cu-action-sheet-content
  position relative
  width 100%
  font-size action-sheet-font-size
  background action-sheet-bg
  text-align center
  overflow auto

.cu-action-sheet-header
  position relative
  vertical-height(action-sheet-height)
  hairline(bottom, color-border-base)
  word-ellipsis()
  overflow visible

.cu-action-sheet-item
  position relative
  vertical-height(action-sheet-height)
  padding 0 action-sheet-padding-h
  box-sizing border-box
  font-size action-sheet-font-size
  transition background-color .3s
  -webkit-user-select none
  &.active
    color action-sheet-color-highlight
  &.disabled .cu-action-sheet-item-section
    opacity action-sheet-disabled-opacity
  &:first-of-type
    .cu-action-sheet-item-wrapper:after
      display none
  &:active
    background-color color-bg-tap
    &.disabled
      background-color transparent

.cu-action-sheet-item-wrapper
  position relative
  hairline(top, color-border-base)

.cu-action-sheet-cancel
  height 132px
  line-height 120px
  color action-sheet-color-cancel
  font-weight font-weight-medium
  &::before
    display block
    content ''
    height 12px
    background action-sheet-cancel-gap-bg
